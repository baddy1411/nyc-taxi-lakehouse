# NYC Taxi Lakehouse — Python dependencies
# Pin major versions; let patch versions float for security updates

# ── Core data stack ───────────────────────────────────────────────
duckdb>=0.10.0,<1.0.0
pyarrow>=15.0.0,<17.0.0
pandas>=2.1.0,<3.0.0
numpy>=1.26.0,<2.0.0

# ── Data validation ───────────────────────────────────────────────
pydantic>=2.5.0,<3.0.0

# ── HTTP / ingestion ──────────────────────────────────────────────
requests>=2.31.0,<3.0.0

# ── dbt ───────────────────────────────────────────────────────────
dbt-core>=1.7.0,<2.0.0
dbt-duckdb>=1.7.0,<2.0.0

# ── Orchestration ─────────────────────────────────────────────────
apache-airflow>=2.8.0,<3.0.0
apache-airflow-providers-http>=4.7.0

# ── Dashboard ────────────────────────────────────────────────────
streamlit>=1.30.0,<2.0.0
plotly>=5.18.0,<6.0.0

# ── Testing ───────────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0          # parallel test execution

# ── Linting / type checking ───────────────────────────────────────
ruff>=0.2.0
mypy>=1.8.0
pandas-stubs>=2.1.0

# ── Optional: cloud backends ──────────────────────────────────────
# boto3>=1.34.0               # AWS S3 backend
# google-cloud-storage>=2.14  # GCS backend
# azure-storage-blob>=12.19   # Azure backend
